<%= link_to gravatar_for(current_user, size:80), current_user %>
<h1><%= current_user.name %></h1>

<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
<span><%= link_to "Follow", followings_user_path(current_user) %>, 
      <%= link_to "Folllower", followers_user_path(current_user) %>
      <%= link_to "Favorite", favorite_user_path(current_user) %></span>
